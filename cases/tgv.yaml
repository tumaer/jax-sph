extends: JAX_SPH_DEFAULTS

case:
  source: "tgv.py"
  dim: 2
  dx: 0.02
  special:
    num_vortices: 2  # per dimension
    V0: 1.0

solver:
  t_end: 5.0

io:
  write_type: ["h5"]
  write_every: 100
  data_path: "data/debug"
